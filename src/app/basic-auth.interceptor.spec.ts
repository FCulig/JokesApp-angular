import { BasicAuth.Interceptor } from './basic-auth.interceptor';

describe('BasicAuth.Interceptor', () => {
  it('should create an instance', () => {
    expect(new BasicAuth.Interceptor()).toBeTruthy();
  });
});
